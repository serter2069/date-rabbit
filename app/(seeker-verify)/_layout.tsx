import { Stack } from "expo-router";

export default function SeekerVerifyLayout() {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
        gestureEnabled: false,
      }}
    />
  );
}
